<%= provide(:title, "All Launches") %>
<h1>All Launches</h1>

<h4><%= link_to "New Launches", new_launch_path %></h4>

<table border="1">
  <thead>
    <td>Data</td>
    <td>Miles</td>
    <td>Price</td>
    <td>Total</td>
  </thead>

  <% @launches.each do |launches| %>
  <tr>
    <td><%= launches.date_launche.to_s %></td>
    <td><%= launches.miles.to_s %></td>
    <td><%= launches.price.to_s %></td>
    <td><%= launches.total.to_s %></td>
  </tr>
  <% end %>
</table>

