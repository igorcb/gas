 <tr>
  <td><%= link_to launche.id, launch_path(launche) %>  </td>
  <td><%= launche.date_launche.to_s %></td>
  <td><%= launche.miles.to_s %></td>
  <td><%= launche.price.to_s %></td>
  <td><%= launche.total.to_s %></td>
  <td>
      <%= link_to 'Destroy', launche, :method => :delete, :confirm => 'Are you sure?', :class => 'btn btn-mini btn-danger' %>
  </td>                                  
</tr>
 
 